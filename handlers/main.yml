---
# handlers file for vector-role
---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart Vector
  ansible.builtin.systemd:
    name: vector
    state: restarted
